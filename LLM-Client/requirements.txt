# batch_pipeline — Python dependencies
# The batch pipeline orchestrates all layers via subprocess.
# Install dependencies for each individual layer separately.
#
# Quick install all layers:
#   pip install -r layer1_vision/requirements.txt
#   pip install -r layer2_pipeline/requirements.txt
#   pip install -r layer3_scoring/requirements.txt
#
# batch_pipeline.py itself has no additional dependencies beyond stdlib.

# Python >= 3.9 required
